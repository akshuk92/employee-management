spring:
  profiles: production
  datasource:
    url: jdbc:mysql://db-prod.internal:3306/employee_db
    username: prod_user
    password: ${DB_PASSWORD}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

server:
  port: 8080

logging:
  level:
    root: WARN
    com.company.employeemanagement: INFO
